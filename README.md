# thr-int

The live (beta) of this code is linked below. However, it is behind until I fix the production configuration. Cloning this 'development' branch and running:

<code>npm run nstart</code>

will obviously create 'this' version locally on localhost:8080
http://thoughtrender.com/betasite
